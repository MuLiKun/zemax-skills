# 公差分析工具_开发进度与测试记录

本文档用于记录公差分析工具的开发进度、阶段任务状态、测试脚本、真实回归记录与后续验证清单。

需求背景、产品形态、功能路线详见：

- [公差分析工具_泛化需求文档.md](公差分析工具_泛化需求文档.md)

---

## 1. 当前开发进度

当前整体开发进度约 **91%**。

当前阶段：**第一阶段：稳定性修复 / 内部试用准备**。

阶段判断：

- 高级 Excel 模式主链路已完成真实 Zemax 小样本端到端验证；
- 一阶段核心稳定性能力基本完成；
- 当前剩余重点是更多样本镜头回归和一阶段收尾审查；
- 第二阶段普通模式尚未正式启动。

---

## 2. 第一阶段任务状态

| 序号 | 任务 | 当前状态 |
|---|---|---|
| 1 | 保留 TSC UTF-16 修复 | 已完成 |
| 2 | 建立本地回滚机制（本地 Git + 备份快照） | 已完成基础版：当前使用本地 Git 管理，并提供 `make_backup.py` 生成 `_backups/YYYYMMDD_HHMMSS/` 快照 |
| 3 | 实现工作副本，避免修改原始 zmx | 已完成，真实运行已验证 |
| 4 | 输出目录时间戳隔离 | 已完成，真实运行已验证 |
| 5 | 支持可选保存 MC case，并限制默认生成文件数量 | 已完成，真实运行已验证 20 次保存 20 个样本及 Worst/Best |
| 6 | 增加运行前校验 | 已完成，支持 `--validate-only`，校验路径、运行参数、MFE/REPORT 映射 |
| 7 | 增加基础测试用例与试用用户验证清单 | 已完成基础版：`check_stage1.py` 覆盖编译、模板生成、配置校验正向和典型负向场景；GUI 真实端到端回归已通过；更多样本镜头后续补充 |
| 8 | 保存 run.log | 已完成，CLI/GUI 自动分析均写入运行日志 |
| 9 | 保存运行配置快照与 Excel 配置快照 | 已完成，输出 `run_config.json` 与 `used_excel.xlsx` |
| 10 | 自动统计 ZTD 时限制为自定义脚本 + REPORT 项 | 已完成，避免自动统计输出大量 `colN` 灵敏度列 |

---

## 3. 第一阶段已验证内容

```text
真实样本：F:\个人文件\P3111\05304.zmx
配置文件：F:\个人文件\P3111\tol_config_模板.xlsx
运行模式：standalone
Monte Carlo：20 次
验证结果：CLI 与 GUI 均已跑通；TDE/MFE/TSC 写入、ZTD 保存、统计 Excel、MC case、Worst/Best、run.log、run_config.json、used_excel.xlsx 均生成成功。

GUI 回归结果目录：F:\\个人文件\\P3111\\公差\\公差分析_05304_20260626_000019
GUI 统计结果：自动统计 13 项（自定义脚本 + 12 个 REPORT），未输出大量无标签 colN。
```

---

## 4. 基础检查脚本

脚本路径：

- [check_stage1.py](check_stage1.py)

运行命令：

```powershell
..\.venv\Scripts\python.exe -u tolerance_analysis\check_stage1.py --python ..\.venv\Scripts\python.exe
```

脚本当前不连接 Zemax，覆盖：

1. Python 编译检查；
2. 临时 Excel 模板生成；
3. `--validate-only` 正向校验；
4. 缺失 zmx 的负向校验；
5. 缺失 Excel 的负向校验；
6. 保存数量大于 MC 次数的负向校验；
7. REPORT 指向不存在 MFE 行号的负向校验。

最近验证结果：**通过**。

---

## 5. 本地备份快照脚本

脚本路径：

- [make_backup.py](make_backup.py)

运行命令：

```powershell
..\.venv\Scripts\python.exe -u tolerance_analysis\make_backup.py
```

默认输出目录：

```text
_backups/YYYYMMDD_HHMMSS/
```

备份内容包括关键代码、`toltool/`、方案入口、构建脚本、配置模板和主要文档。`_backups/` 已加入 `.gitignore`，默认不入库。

最近验证结果：**通过**，已生成一次本地快照。

---

## 6. GUI 真实端到端回归清单

| 步骤 | 检查项 | 通过标准 |
|---|---|---|
| 1 | 启动 GUI | 程序可打开，无启动异常 |
| 2 | 选择 zmx | 能选择真实镜头文件，路径显示正确 |
| 3 | 选择 Excel | 能选择配置 Excel，路径显示正确 |
| 4 | 选择输出目录 | 结果目录按用户选择或镜头目录生成 |
| 5 | 选择连接模式 | standalone 模式可启动 Zemax 独立实例 |
| 6 | 开始运行 | 日志窗口持续输出关键阶段：结果目录、工作副本、TDE、MFE、TSC、蒙特卡洛进度 |
| 7 | 检查结果目录 | 生成工作副本、ZTD、统计 Excel、MC case、Worst/Best、`run.log`、`run_config.json`、`used_excel.xlsx` |
| 8 | 检查统计 Excel | 自动统计只包含自定义脚本 + Excel 启用的 REPORT 项，不输出大量无标签 `colN` |
| 9 | 检查日志 | GUI 日志窗口和 `run.log` 均包含成功/失败关键信息 |
| 10 | 异常场景 | 缺失 zmx、缺失 Excel 或配置错误时能给出明确错误，不误判为成功 |

---

## 7. GUI 回归执行记录

| 日期 | 样本 zmx | Excel 配置 | 连接模式 | MC 次数 | 结果 | 备注 |
|---|---|---|---|---:|---|---|
| 2026-06-26 | `F:\\个人文件\\P3111\\05304.zmx` | `F:\\个人文件\\P3111\\tol_config_模板.xlsx` | standalone | 20 | 通过 | 结果目录：`F:\\个人文件\\P3111\\公差\\公差分析_05304_20260626_000019`；自动统计 13 项；ZTD、统计 Excel、20 个 MC case、Worst/Best、run.log、run_config.json、used_excel.xlsx 均生成 |

---

## 8. 当前代码改造优先级与状态

| 优先级 | 改造项 | 当前状态 | 价值 |
|---|---|---|---|
| P0 | TSC UTF-16 | 已完成 | 解决中文 TSC 兼容性 |
| P0 | 本地回滚机制 | 已完成基础版 | 当前已使用本地 Git 管理，并提供 `_backups` 快照脚本 |
| P0 | 工作副本隔离 | 已完成 | 避免修改原始 zmx，真实运行已验证 |
| P0 | 运行前配置校验 | 已完成 | 支持 `--validate-only`，减少同事误操作 |
| P0 | run.log / run_config / used_excel 追溯 | 已完成 | 便于复盘每次运行的输入、参数和错误信息 |
| P0 | 自动统计列限制 | 已完成 | 自动分析只统计自定义脚本 + REPORT 项，避免输出大量 `colN` |
| P1 | 输出目录结构化 | 已完成基础版 | 当前采用时间戳结果目录，后续可再细分 work/report/config/log 子目录 |
| P1 | MC case 可选保存策略 | 已完成基础版 | 可保存指定数量 MC case 及 Worst/Best，真实运行已验证 |
| P1 | 测试用例与试用验证 | 已完成基础版 | 基础检查脚本、典型负向场景和 GUI 真实回归已通过，后续补多镜头样本 |
| P1 | 普通标准模板模式 | 未开始 | 大幅降低 Excel 依赖 |
| P2 | 使用 Zemax 当前 TDE/MFE | 未开始 | 提升泛用性 |
| P2 | 自动 REPORT 生成 | 未开始 | 简化 MFE 行号配置 |
| P2 | 指向角/FOV 评价项 | 未开始 | 支持视轴稳定性评估 |
| P3 | 模板管理 | 未开始 | 支持多项目复用 |
| P4 | 公差模板识别模式 | 未开始 | 识别结构同事提供的公差资料，用户确认后生成 Excel 或 TDE |

---

## 9. 下一步建议

一阶段剩余收尾建议顺序：

1. 多镜头样本回归；
2. 一阶段收尾审查；
3. 提交/推送一阶段收尾改动。
